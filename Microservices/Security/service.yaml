apiVersion: v1
kind: Service
metadata:
  name: security-service
  labels:
    app: security
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  selector:
    app: security
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP